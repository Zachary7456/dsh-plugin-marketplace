# API 设计要点

- **命名**：动词 + 资源、复数资源名；避免缩写
- **版本**：通过路径或头字段显式版本化
- **错误**：统一错误结构（code/message/details），HTTP 状态码语义正确
- **幂等**：PUT/DELETE 幂等；写操作支持幂等键
- **分页**：cursor 优先于 offset；返回总数可选
- **兼容**：字段只增不改；破坏性变更走新版本

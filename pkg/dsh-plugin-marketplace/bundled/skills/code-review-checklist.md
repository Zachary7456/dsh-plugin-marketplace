# 代码审查清单

审查代码改动时逐项检查：

- **正确性**：边界条件、空值、并发与错误路径是否处理
- **安全**：输入校验、注入风险、敏感信息是否泄漏
- **性能**：明显的 O(n²) 或重复 IO 是否可避免
- **一致性**：命名与既有代码风格一致，错误处理方式一致
- **测试**：关键分支是否有测试覆盖
- **变更范围**：是否夹带无关改动

输出时按「必须修改 / 建议修改 / 疑问」分组，每条给出文件位置与理由。
